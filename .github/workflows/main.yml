name: CherryOS CI/CD Pipeline

on:
  push:
    branches: [ main ]
  workflow_dispatch:
  schedule:
    - cron: '0 0,12 * * *' 

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    name: Build, Test, & Deploy
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Run Linter
        run: npm run lint

      - name: Run Tests
        run: npm test -- --run

      - name: Fetch Live API Data (Dynamic Pre-build)
        env:
          VITE_STEAM_ID: "76561198043191125"
          VITE_STEAM_API_KEY: ${{ secrets.VITE_STEAM_API_KEY }}
          VITE_AL_USERNAME: "AugustElliott"
          VITE_AL_ID: "36296"
        run: |
          mkdir -p public/data/mirror
          node -e "
          const fs = require('fs');
          const https = require('https');
          
          async function request(url, options = {}) {
            return new Promise((resolve, reject) => {
              const req = https.request(url, options, (res) => {
                let body = '';
                res.on('data', (chunk) => body += chunk);
                res.on('end', () => {
                  if (res.statusCode >= 200 && res.statusCode < 300) {
                    try { resolve(JSON.parse(body)); } catch (e) { reject(new Error('JSON Parse Error: ' + e.message)); }
                  } else {
                    reject(new Error(\`HTTP Status \${res.statusCode}: \${body}\`));
                  }
                });
              });
              req.on('error', reject);
              if (options.body) req.write(options.body);
              req.end();
            });
          }

          const FAVORITES = [
            'gengar', 'mimikyu-disguised', 'scizor', 'chimecho', 'meganium', 'shuckle', 
            'jirachi', 'celebi', 'swampert', 'tyranitar', 'snorlax', 'mienshao', 'raikou', 
            'zapdos', 'regice', 'lugia', 'espeon', 'altaria', 'skarmory', 'heracross', 
            'cofagrigus', 'kabutops', 'genesect', 'ampharos', 'politoed', 
            'hitmontop', 'cradily', 'milotic', 'metagross', 'latias', 'magnezone', 
            'togekiss', 'rotom', 'gliscor', 'aegislash-shield', 'goodra'
          ];

          async function run() {
            try {
              // 1. Pokedex Mirror (Concurrent)
              console.log('Mirroring Pokedex...');
              const moveCache = new Map(); // Cache move details to avoid redundant requests

              const pokedex = await Promise.all(FAVORITES.map(async (name) => {
                try {
                  const data = await request(\`https://pokeapi.co/api/v2/pokemon/\${name}\`);
                  const species = await request(data.species.url);
                  const description = species.flavor_text_entries
                    .find(e => e.language.name === 'en')
                    ?.flavor_text.replace(/[\\n\\f]/g, ' ') || '';
                  
                  const pokemonMoves = {};
                  // Use a selection of level-up moves to keep build fast and relevant
                  const levelUpMoves = data.moves.filter(m => 
                    m.version_group_details.some(d => d.move_learn_method.name === 'level-up')
                  ).slice(0, 50); 

                  for (const m of levelUpMoves) {
                    // Resolve move details (cached)
                    let moveInfo = { type: 'normal', accuracy: null, power: null };
                    if (moveCache.has(m.move.name)) {
                      moveInfo = moveCache.get(m.move.name);
                    } else {
                      try {
                        const moveDetails = await request(m.move.url);
                        moveInfo = {
                          type: moveDetails.type.name,
                          accuracy: moveDetails.accuracy,
                          power: moveDetails.power
                        };
                        moveCache.set(m.move.name, moveInfo);
                      } catch(e) {}
                    }

                    m.version_group_details.forEach(d => {
                      if (d.move_learn_method.name !== 'level-up') return;
                      const v = d.version_group.name;
                      if (!pokemonMoves[v]) pokemonMoves[v] = [];
                      pokemonMoves[v].push({
                        name: m.move.name.replace(/-/g, ' '),
                        level: d.level_learned_at,
                        method: d.move_learn_method.name,
                        type: moveInfo.type,
                        accuracy: moveInfo.accuracy,
                        power: moveInfo.power
                      });
                    });
                  }

                  const spriteBase = data.name.replace(/-disguised|-galar|-shield/g,'');
                  return {
                    id: data.id,
                    name: data.name,
                    types: data.types.map(t => t.type.name),
                    stats: data.stats.map(s => ({ name: s.stat.name, value: s.base_stat })),
                    height: data.height,
                    weight: data.weight,
                    description,
                    sprite: \`https://play.pokemonshowdown.com/sprites/gen5ani/\${spriteBase}.gif\`,
                    shinySprite: \`https://play.pokemonshowdown.com/sprites/gen5ani-shiny/\${spriteBase}.gif\`,
                    artwork: \`https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/\${data.id}.png\`,
                    footprint: \`https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/footprints/\${data.id}.png\`,
                    moves: pokemonMoves
                  };
                } catch(e) { console.error(\`Failed \${name}\`); return null; }
              }));
              fs.writeFileSync('public/data/mirror/pokedex.json', JSON.stringify(pokedex.filter(p => p !== null)));

              // 2. AniList Mirror
              console.log('Mirroring AniList...');
              let aniData = null;
              try {
                aniData = await request('https://graphql.anilist.co', {
                  method: 'POST',
                  headers: { 'Content-Type': 'application/json' },
                  body: JSON.stringify({
                    query: 'query(\$username:String){MediaListCollection(userName:\$username,type:ANIME){lists{name status entries{updatedAt media{id duration episodes genres title{english romaji}coverImage{extraLarge}averageScore type status}score(format:POINT_10)progress status}}}MangaList:MediaListCollection(userName:\$username,type:MANGA){lists{name status entries{updatedAt media{id duration episodes genres title{english romaji}coverImage{extraLarge}averageScore type status}score(format:POINT_10)progress status}}}}',
                    variables: { username: 'AugustElliott' }
                  })
                });
              } catch(e) { }
              fs.writeFileSync('public/data/mirror/anilist.json', JSON.stringify(aniData || { data: { MediaListCollection: { lists: [] }, MangaList: { lists: [] } } }));

              // 3. Steam Mirror (Optimized)
              console.log('Mirroring Steam...');
              let steamData = [];
              try {
                const key = process.env.VITE_STEAM_API_KEY;
                const id = process.env.VITE_STEAM_ID;
                if (!key) throw new Error('Steam key missing');
                const raw = await request(\`https://api.steampowered.com/IPlayerService/GetOwnedGames/v0001/?key=\${key}&steamid=\${id}&format=json&include_appinfo=true&include_played_free_games=true\`);
                if (raw.response && raw.response.games) {
                  const games = raw.response.games;
                  // Map all owned games
                  steamData = games.map(g => ({
                    id: g.appid,
                    title: g.name,
                    playtime_raw: g.playtime_forever,
                    playtime: Math.floor(g.playtime_forever / 60) + 'h ' + (g.playtime_forever % 60) + 'm',
                    achievementPercent: 0, // Default to 0, will enrich top games
                    coverImage: \`https://cdn.akamai.steamstatic.com/steam/apps/\${g.appid}/header.jpg\`,
                    lastPlayed: g.rtime_last_played
                  }));

                  // Enrich top 100 games with achievements concurrently
                  const topGames = [...steamData].sort((a,b) => b.playtime_raw - a.playtime_raw).slice(0, 100);
                  await Promise.all(topGames.map(async (tg) => {
                    try {
                      // Small delay to avoid hitting Steam API rate limits too hard if needed, 
                      // but usually 100 concurrent requests is fine for Steam API
                      const ach = await request(`https://api.steampowered.com/ISteamUserStats/GetPlayerAchievements/v0001/?appid=${tg.id}&key=${key}&steamid=${id}`);
                      if (ach.playerstats && ach.playerstats.achievements) {
                        const total = ach.playerstats.achievements.length;
                        const won = ach.playerstats.achievements.filter(a => a.achieved === 1).length;
                        const gameIdx = steamData.findIndex(item => item.id === tg.id);
                        if (gameIdx !== -1) {
                          steamData[gameIdx].achievementPercent = total > 0 ? (won / total) * 100 : 0;
                          steamData[gameIdx].achievements = { won, total };
                        }
                      }
                    } catch(e) {}
                  }));

                }
              } catch(e) { console.error('Steam failed:', e.message); }
              fs.writeFileSync('public/data/mirror/steam.json', JSON.stringify({ data: steamData }));

              fs.writeFileSync('public/data/mirror/metadata.json', JSON.stringify({ lastUpdated: new Date().toISOString() }));
              console.log('Mirror complete.');
            } catch (err) {
              console.error('Mirror engine failure:', err.message);
              process.exit(1);
            }
          }
          run();
          "

      - name: Build Application
        env:
          VITE_WEATHER_API_KEY: ${{ secrets.VITE_WEATHER_API_KEY }}
          VITE_OCI_NAMESPACE: ${{ secrets.OCI_NAMESPACE }}
          VITE_OCI_REGION: ${{ secrets.OCI_REGION }}
          VITE_AL_ID: "36296"
          VITE_STEAM_ID: "76561198043191125"
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          commit_message: 'Automated Deployment via GitHub Actions [skip ci]'

      - name: Sync Artifacts to OCI
        env:
          OCI_API_KEY: ${{ secrets.OCI_API_KEY }}
          OCI_USER_OCID: ${{ secrets.OCI_USER_OCID }}
          OCI_FINGERPRINT: ${{ secrets.OCI_FINGERPRINT }}
          OCI_TENANCY_OCID: ${{ secrets.OCI_TENANCY_OCID }}
          OCI_REGION: ${{ secrets.OCI_REGION }}
          OCI_NAMESPACE: ${{ secrets.OCI_NAMESPACE }}
        run: |
          mkdir -p ~/.oci
          echo "$OCI_API_KEY" > ~/.oci/oci_api_key.pem
          chmod 600 ~/.oci/oci_api_key.pem
          cat <<EOF > ~/.oci/config
          [DEFAULT]
          user=$OCI_USER_OCID
          fingerprint=$OCI_FINGERPRINT
          tenancy=$OCI_TENANCY_OCID
          region=$OCI_REGION
          key_file=~/.oci/oci_api_key.pem
          EOF
          chmod 600 ~/.oci/config
          bash -c "$(curl -L https://raw.githubusercontent.com/oracle/oci-cli/master/scripts/install/install.sh)" -- --accept-all-defaults
          echo "/home/runner/bin" >> $GITHUB_PATH
          /home/runner/bin/oci os object bulk-upload -ns $OCI_NAMESPACE -bn cherryos-deploy-prod --src-dir ./dist --overwrite
          /home/runner/bin/oci os object put -ns $OCI_NAMESPACE -bn cherryos-deploy-prod --file healthcheck.txt --name healthcheck.txt --force

      - name: Login to OCI Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.OCI_REGISTRY_URL }}
          username: ${{ secrets.OCI_USERNAME }}
          password: ${{ secrets.OCI_AUTH_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.OCI_REGISTRY_URL }}/${{ secrets.OCI_NAMESPACE }}/cherryos:latest
          build-args: |
            VITE_WEATHER_API_KEY=${{ secrets.VITE_WEATHER_API_KEY }}
            VITE_OCI_NAMESPACE=${{ secrets.OCI_NAMESPACE }}
            VITE_OCI_REGION=${{ secrets.OCI_REGION }}
            VITE_STEAM_ID=76561198043191125
            VITE_AL_ID=36296
